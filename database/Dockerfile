FROM postgres:16-alpine

# Optional: Add initialization scripts here if needed
# Scripts in /docker-entrypoint-initdb.d/ will be executed on first run

# The database will be initialized automatically by the application
# This Dockerfile is provided for customization purposes
